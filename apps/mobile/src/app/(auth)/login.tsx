import { View, Text } from "react-native";

/** Placeholder — implemented from Figma (Log in 8:2082 / 36:2284) in Phase 1. */
export default function Login() {
  return (
    <View className="flex-1 items-center justify-center bg-bg">
      <Text className="font-medium text-ink">Login — Phase 1</Text>
    </View>
  );
}
