import type { Availability } from "@arteesans/shared";

export const AVAILABILITY_LABELS: Record<Availability, string> = {
  full_time: "Full time",
  part_time: "Part time",
  weekends_only: "Weekends only",
  flexible: "Flexible",
  on_demand: "On demand",
};
